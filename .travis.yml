sudo: required

language: php

php:
  - 5.5.9

before_script:
  - cp .env.example .env
  - composer install --no-interaction
  - npm install -g yarn
  - sudo apt-get install -y node
  - node -v
  - yarn
  - php artisan key:generate
  - gulp


script:
  - vendor/bin/phpunit
