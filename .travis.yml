language: php

php:
  - 5.5.9

sudo: false

before_script:
  - cp .env.example .env
  - composer install --no-interaction
  - npm install -g yarn
  - yarn
  - php artisan key:generate
  - gulp


script:
  - vendor/bin/phpunit
