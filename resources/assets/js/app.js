/**
 * Precisamos incluir todas as dependências Javascript do
 * projeto e inicializa-las.
 */
require('./bootstrap');

/**
 * Inicializamos os elementos que utilizamos dentro do sistema
 * para todas as páginas.
 */
require('./components/charts');
